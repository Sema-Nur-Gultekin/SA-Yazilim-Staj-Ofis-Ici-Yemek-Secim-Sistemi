@{
    Layout = null;
    ViewBag.Title = "Yeni Şifre Oluştur";
    bool invalid = ViewBag.Invalid != null && (bool)ViewBag.Invalid;
    bool done = ViewBag.Done != null && (bool)ViewBag.Done;
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Ofis Yemek Seçim</title>
    @Styles.Render("~/Content/css")
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: linear-gradient(160deg, #f1f5f9 0%, #e2e8f0 50%, #f8fafc 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .login-container {
            width: 100%;
            max-width: 440px;
            padding: 20px;
        }

        .login-card {
            background: white;
            border-radius: 24px;
            box-shadow: 0 25px 60px rgba(0, 0, 0, 0.08), 0 5px 15px rgba(0, 0, 0, 0.03);
            padding: 3rem 2.5rem;
            position: relative;
            overflow: hidden;
        }

            .login-card::before {
                content: '';
                position: absolute;
                top: 0;
                left: 50%;
                transform: translateX(-50%);
                width: 60px;
                height: 4px;
                background: linear-gradient(90deg, #3b82f6, #60a5fa);
                border-radius: 0 0 4px 4px;
            }

        .login-brand {
            text-align: center;
            margin-bottom: 2rem;
        }

        .logo-wrapper {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 80px;
            height: 80px;
            background: linear-gradient(145deg, #ffffff 0%, #f8fafc 100%);
            border-radius: 50%;
            margin-bottom: 1.25rem;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.08), 0 2px 8px rgba(0, 0, 0, 0.04), inset 0 2px 4px rgba(255, 255, 255, 0.9);
        }

        .logo-icon {
            font-size: 34px;
            color: #3b82f6;
        }

        .brand-name {
            font-size: 1.4rem;
            font-weight: 700;
            color: #1e293b;
            margin-bottom: 0.4rem;
        }

        .brand-tagline {
            font-size: 0.85rem;
            color: #64748b;
            line-height: 1.5;
        }

        .form-group {
            margin-bottom: 1.25rem;
        }

        .form-label {
            display: block;
            font-size: 0.813rem;
            font-weight: 600;
            color: #475569;
            margin-bottom: 0.5rem;
        }

        .form-input {
            width: 100%;
            max-width: 100%;
            display: block;
            padding: 0.875rem 1rem;
            border: 2px solid #e2e8f0;
            border-radius: 12px;
            font-size: 0.938rem;
            color: #1e293b;
            transition: all 0.2s ease;
            background: #f8fafc;
            box-sizing: border-box;
        }

            .form-input:focus {
                outline: none;
                border-color: #3b82f6;
                background: white;
                box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.08);
            }

        .alert {
            background: #fef2f2;
            border: 1px solid #fecaca;
            border-left: 3px solid #ef4444;
            color: #dc2626;
            padding: 0.875rem 1rem;
            border-radius: 10px;
            font-size: 0.813rem;
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 0.625rem;
        }

        .alert-success {
            background: #f0fdf4;
            border: 1px solid #bbf7d0;
            border-left: 3px solid #22c55e;
            color: #15803d;
        }

        .btn-login {
            width: 100%;
            padding: 0.875rem;
            background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
            color: white;
            border: 2px solid transparent;
            border-radius: 12px;
            font-size: 0.938rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-top: 0.5rem;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            text-decoration: none;
            display: block;
            text-align: center;
        }

            .btn-login:disabled {
                opacity: 0.7;
                cursor: not-allowed;
            }

            .btn-login:hover:not(:disabled) {
                transform: translateY(-2px);
                box-shadow: 0 10px 25px rgba(59, 130, 246, 0.35), 0 4px 10px rgba(37, 99, 235, 0.2);
            }

        .login-footer {
            display: flex;
            justify-content: center;
            margin-top: 1.75rem;
            padding-top: 1.75rem;
            border-top: 1px solid #f1f5f9;
        }

        .link-back {
            font-size: 0.813rem;
            color: #64748b;
            text-decoration: none;
            font-weight: 500;
        }

            .link-back:hover {
                color: #3b82f6;
            }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="login-card">

            <div class="login-brand">
                <div class="logo-wrapper">
                    <i class="fa-solid fa-lock logo-icon"></i>
                </div>
                <h1 class="brand-name">Yeni Şifre Oluştur</h1>
                @if (!invalid && !done)
                {
                    <p class="brand-tagline">Hesabınız için yeni bir şifre belirleyin.</p>
                }
            </div>

            @if (invalid)
            {
                <div class="alert">
                    <i class="fa-solid fa-circle-exclamation"></i>
                    Bu şifre sıfırlama bağlantısı geçersiz, daha önce kullanılmış veya süresi dolmuş (30 dakika). Lütfen yeni bir sıfırlama bağlantısı isteyin.
                </div>
                <a href="@Url.Action("ForgotPassword", "Account")" class="btn-login">
                    <i class="fa-solid fa-rotate-right"></i> Yeni Bağlantı İste
                </a>
            }
            else if (done)
            {
                <div class="alert alert-success">
                    <i class="fa-solid fa-circle-check"></i>
                    Şifreniz başarıyla güncellendi. Artık yeni şifrenizle giriş yapabilirsiniz.
                </div>
                <a href="@Url.Action("Login", "Account")" class="btn-login">
                    <i class="fa-solid fa-right-to-bracket"></i> Giriş Yap
                </a>
            }
            else
            {
                if (ViewBag.Error != null)
                {
                    <div class="alert">
                        <i class="fa-solid fa-circle-exclamation"></i>
                        @ViewBag.Error
                    </div>
                }

                <form method="post" action="@Url.Action("ResetPassword", "Account")" id="resetForm">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="token" value="@ViewBag.Token" />

                    <div class="form-group">
                        <label class="form-label" for="password">Yeni Şifre</label>
                        <input type="password"
                               name="password"
                               id="password"
                               class="form-input"
                               placeholder="En az 6 karakter"
                               minlength="6"
                               required
                               autofocus />
                    </div>

                    <div class="form-group">
                        <label class="form-label" for="confirmPassword">Yeni Şifre (Tekrar)</label>
                        <input type="password"
                               name="confirmPassword"
                               id="confirmPassword"
                               class="form-input"
                               placeholder="Şifrenizi tekrar girin"
                               minlength="6"
                               required />
                    </div>

                    <button type="submit" class="btn-login" id="submitBtn">
                        <i class="fa-solid fa-check"></i> Şifreyi Güncelle
                    </button>
                </form>
            }

            <div class="login-footer">
                <a href="@Url.Action("Login", "Account")" class="link-back">
                    <i class="fa-solid fa-arrow-left"></i> Giriş sayfasına dön
                </a>
            </div>

        </div>
    </div>

    <script>
       
        var form = document.getElementById('resetForm');
        if (form) {
            form.addEventListener('submit', function () {
                var btn = document.getElementById('submitBtn');
                btn.disabled = true;
                btn.innerHTML = 'Kaydediliyor...';
            });
        }
    </script>
</body>
</html>
